# v0.2 Media Placeholder

Purpose: reserve reproducible media paths for v0.2 release packaging.

Status:
- LED behavior video/GIF is not attached yet.
- This placeholder keeps a stable public path for later upload.

Expected assets:
- `recovery-idle-violet-blink.mp4` (or `.gif`)
- `selector-update-recovery-normal.mp4` (optional)

Capture note:
- Media is non-blocking for v0.2 protocol validation.
- Protocol claims are backed by serial logs under `docs/evidence/v0.2/logs/`.
